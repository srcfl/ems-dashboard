# SEL Backend build configuration for Railway
[phases.setup]
nixPkgs = ["rustc", "cargo", "openssl", "pkg-config"]

[phases.build]
cmds = ["cargo build --release --features server"]

[start]
cmd = "./target/release/sel-server"

[variables]
RUST_LOG = "sel_server=info,tower_http=info"
